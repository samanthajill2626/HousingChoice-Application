# Known Issues — moved

This file has been superseded by the in-repo **issue registry** at
[`docs/issues/`](./issues/) (one file per issue + a generated index). See
[`docs/issues/README.md`](./issues/README.md) for how it works, or run `npm run issues`.

The two findings formerly tracked here were migrated 2026-06-18 (both resolved):

- [`media-serve-stored-xss`](./issues/media-serve-stored-xss.md) — stored XSS via served
  MMS `Content-Type` (was item #1).
- [`boards-relay-intro-flake`](./issues/boards-relay-intro-flake.md) — `boards.spec.ts`
  relay-intro e2e failure (was item #2).

_(This stub is kept so existing links resolve; new issues go in `docs/issues/`, not here.)_
