// App — the root component. For B0.2 this is a smoke placeholder that renders
// the HousingChoice brand; B0.3 wraps it in the auth gate and B0.4 introduces
// the AppFrame + routes.
export default function App(): React.JSX.Element {
  return (
    <main>
      <h1>HousingChoice</h1>
    </main>
  );
}
