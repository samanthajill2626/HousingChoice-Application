// Smoke version — the full app shell (DevBanner + RosterRail + PhonePanel +
// Composer, wired to the live data hook) is built in Phase F/G.
export function App(): React.JSX.Element {
  return (
    <div>
      <h1>Fake Phones</h1>
    </div>
  );
}
