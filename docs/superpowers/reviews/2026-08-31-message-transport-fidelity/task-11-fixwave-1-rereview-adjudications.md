# Task 11 fix wave 1 rereview adjudications

## P1 collapsed accessible summary: accepted

Thread the parent schema version, or a derived compatibility boolean, through
`recipientSummaryName` and every collapsed summary caller. Legacy parent messages
must not append recipient transport; version-1 absent recipient facts remain
`Unknown`. Add exact accessible-name regressions for legacy Relay/native Group and
a version-1 unresolved control.

## P1 inbound Relay stale assertion: accepted

Rewrite the delivery test to reveal and assert the permitted inbound Relay recipient
list. This is a test-contract correction for already-shipped approved behavior, not
a reversal of the rendering implementation.
