// housingchoice worker — placeholder entrypoint.
// Job handlers and the jobs.enqueue()/defineJobHandler() gates arrive in M0.2.
console.log(
  'housingchoice worker — placeholder entrypoint. Job handlers and jobs.enqueue()/defineJobHandler() gates arrive in M0.2.',
);
process.exit(0);
