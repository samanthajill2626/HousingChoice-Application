# Final spec-conformance review - message transport fidelity

Scope: final diff package plus live tree at `f1325049`; read-only inspection only.
No test suite was launched by this reviewer.

## Verdict

No confirmed feature-code or test-contract deviation. D1, D2, P1, W1-W4, R1,
U1, and E1 CONFORM. E2 is PARTIAL only because the final completion sequence is
still in progress / has a recorded full-E2E exception; it is not a code finding.

## Work-map trace

| Item | Result | Live evidence |
| --- | --- | --- |
| D1 - Domain and evidence | CONFORMS | The app owns the closed transport and aggregation unions plus transition decisions at `app/src/lib/messageTransport.ts:3-64`. The provider-boundary normalizer accepts only documented SID/endpoint/channel evidence, preserves missing evidence, and produces safe conflicts at `app/src/adapters/twilioMessageTransport.ts:99-171`. The webhook wrapper logs only its normalized safe facts at `app/src/routes/webhooks/twilio.ts:362-383`. |
| D2 - Adapter contract | CONFORMS | The carrier adapter declares classify/prepare/execute contracts at `app/src/adapters/messaging.ts:69-84`, preserves the request before execution at `app/src/services/sendMessage.ts:384-420`, and normalizes provider result evidence at `app/src/adapters/messaging.ts:719-749`. The Group MMS adapter owns the rail fact and returns requested/actual MMS at `app/src/adapters/groupConversations.ts:710-838`; services do not infer it from type or content. |
| P1 - Persistence | CONFORMS | Additive message/slot fields are validated (including carrier-only and inbound-request prohibition) at `app/src/repos/messagesRepo.ts:886-936`. Message and recipient actual writes are conditional on version 1 and preserve the allowed RCS-fallback transition at `app/src/repos/messagesRepo.ts:2963-3024` and `3139-3206`. Initialization, aggregation, and send-result writes are child-field conditional operations at `app/src/repos/messagesRepo.ts:3026-3348`; they do not replace a slot. |
| W1 - Direct writers | CONFORMS | Direct send persists the intent plus only adapter-returned actual transport at `app/src/services/sendMessage.ts:384-420`. Inbound normalizes after signature verification at `app/src/routes/webhooks/twilio.ts:1951-1977`. Status callbacks resolve a direct row or Relay pointer before normalizing against stored requested intent at `app/src/routes/webhooks/twilio.ts:2442-2482` and `2531-2621`, then independently apply delivery status and actual evidence. This covers status-lookup normalization and status/write independence. |
| W2 - Relay writers | CONFORMS | The worker has an explicit schema discriminator: schema-absent sources take `runLegacyRelayFanOut` with no transport path at `app/src/jobs/relayFanOut.ts:784-788` and `954-971`. Version-1 execution completes all eligible initialization/reconciliation before its send loop at `app/src/jobs/relayFanOut.ts:1012-1049` and `1256-1321`; each provider call changes a leg to attempted immediately beforehand at `app/src/jobs/relayFanOut.ts:1109-1123`. Persisted announcements seed planned requested slots and apply attempted/excluded/result transitions at `app/src/services/relayAnnouncements.ts:208-240` and `265-359`. |
| W3 - Native group writers | CONFORMS | Group sends obtain the Group adapter intent before posting at `app/src/services/groupSend.ts:396-410`, and persist requested/actual MMS plus attempted or suppressed/excluded slots at `app/src/services/groupSend.ts:624-664`. Group receipts copy the source rail-authoritative actual transport to the addressed slot rather than treating a channel SID as authority at `app/src/services/groupReceipts.ts:394-443`. |
| W4 - Non-live writers | CONFORMS | The seed helper writes only explicitly declared facts and rejects invalid declarations at `app/src/lib/seed/messageTransport.ts:32-63`; lean and performance seeds explicitly declare normal SMS/MMS or dedicated RCS/legacy cases at `app/src/lib/seed/lean.ts:282-389` and `app/src/lib/seed/performance.ts:939-947`, `968-984`. Dev fixtures validate legacy/versioned payloads at `app/src/routes/dev.ts:870-1028`. The fake accepts `ChannelPrefix` only for explicit RCS and serializes metadata once at `fake-twilio/src/engine/signer.ts:28-35`, `58-79`, `203-223`; its e2e fixture repeats that RCS-only guard at `e2e/fixtures/fakeTwilio.ts:39-45`, `102-131`. Evidence-free import code is untouched by transport-field writers, retaining schema-absent history. |
| R1 - Readers | CONFORMS | The contact projection copies fields only when present at `app/src/routes/contactTimeline.ts:426-436`; dashboard wire types mirror message and recipient contracts at `dashboard/src/api/types.ts:1592-1690`; fallback mapping preserves optional values at `dashboard/src/routes/contact/buildTimelineFallback.ts:71-78`. |
| U1 - Presentation | CONFORMS | The pure presenter makes optimistic carrier bubbles label-free, uses schema version as the legacy discriminator, preserves inbound actual-only behavior, and implements expected-slot completeness/Mixed at `dashboard/src/lib/messageTransport.ts:79-131`. The shared inclusion predicate hides only `excluded` slots without an error code at `dashboard/src/lib/messageTransport.ts:43-56`, and Timeline uses it for disclosure, counts, and stale state at `dashboard/src/routes/contact/Timeline.tsx:796-800`, `885-888`, `1094-1100` and `dashboard/src/routes/contact/deliveryStatus.ts:249-254`, `404-409`. Optimistic resolution retains `optimistic: true` until a server item with the resolved ID refetches and replaces it at `dashboard/src/routes/contact/useContactTimeline.ts:317-332`, `553-559` (same pattern in Relay and native-group hooks). |
| E1 - Real-flow proof | CONFORMS | `e2e/tests/dashboard-next/message-transport-fidelity.spec.ts:266-367` proves fallback, complete/incomplete aggregation, inbound actual/Unknown, legacy, excluded-only hiding, optimistic suppression through refetch, native Group MMS, and inbound-Relay main-versus-leg presentation. The mission ledger records its targeted hermetic run green after the assertion fix. |
| E2 - Completion | PARTIAL | The one final `main` merge is present in the reviewed history (`ffa2669c`). Typecheck, test, and smoke exit records are present under `.superpowers/sdd/final-gates/`; however, `progress.md` ends `STATUS: RUNNING final review children spec-conformance and adversarial` and records that the final full E2E run was stopped after two non-feature placement failures. The final E2E exit marker, final all-green gate set, final self-QA record, and handback therefore remain orchestrator-owned pending work. |

## Invariant probes

- Status lookup normalization: PASS. Direct callbacks load their message and Relay callbacks load their pointer/source request before normalization (`app/src/routes/webhooks/twilio.ts:2449-2465`, `2531-2561`, `2595-2611`).
- Schema-absent Relay path: PASS. The branch is made before any versioned helper call (`app/src/jobs/relayFanOut.ts:784-788`), and repository transport writes return `legacy_noop` after a conditional miss (`app/src/repos/messagesRepo.ts:2994-3000`, `3062-3065`, `3173-3182`).
- Eligible preflight scope: PASS. The candidate set removes the sender and applies continuation keys before preflight (`app/src/jobs/relayFanOut.ts:1012-1017`); preflight creates/reconciles only those candidates before provider calls (`1256-1321`).
- Status/write independence: PASS. Both direct and Relay callback paths invoke the actual write even when the status transition is a duplicate/rejection, then publish for either state-machine change (`app/src/routes/webhooks/twilio.ts:2466-2521`, `2612-2662`).
- Group MMS provider rail facts: PASS. The rail boundary states inbound Group MMS at `app/src/adapters/groupConversations.ts:37-46`, and outbound authority remains `actualTransport: 'mms'` at `806-838` rather than being inferred in the UI/service.
- No fabricated `ChannelPrefix`: PASS. RCS-only validation is enforced in both fake provider and e2e fixture cited under W4; ordinary SMS/MMS controls omit it.
- Excluded-only hiding: PASS. The same predicate feeds recipient disclosure, rollups, and stale logic (U1 evidence), while opted-out slots retain their error-backed row.
- Refetch-suppressed optimism: PASS. Local resolve updates only ID/status and keeps the marker; server merge removes the pending row by matching ID (`dashboard/src/routes/contact/useContactTimeline.ts:317-332`, `553-559`).
- Seed evidence: PASS. Explicit helper declarations cover lean, cast, performance, and dev fixtures; no helper derives transport from `type`, media, or conversation kind (`app/src/lib/seed/messageTransport.ts:32-63`).

## Findings

No confirmed must-fix findings. The E2 PARTIAL entry is a completion-state fact,
not a spec-conformance defect in the reviewed code.
