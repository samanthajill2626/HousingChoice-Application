# Task 11 fix wave 3 rereview adjudications

## P1 collapsed inbound Relay disclosure: accepted

Add an always-rendered accessible disclosure for an inbound Relay source that uses
the same filtered recipient rows, parent-schema transport rule, and non-empty leg
times as the revealed rows. It must not make outbound legs replace or alter the
inbound source's actual-only main transport. Cover before-interaction access with
one timed and one untimed leg.
