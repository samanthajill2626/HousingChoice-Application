# Task 12 E2E review adjudications

## P2 exact transport assertions: accepted

Assert the complete bubble-scoped transport segment, including negative transition
coverage for requested-only and agreement states. Preserve separate post-callback
fallback checks so the targeted browser proof demonstrates a real state transition.

## P2 excluded-recipient regex: accepted

Correct the regex escaping or use a clear exact accessible selector. Retain the
zero-count check and controls proving state-absent and opted-out rows remain visible.
