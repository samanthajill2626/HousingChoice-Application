# Planner review fix wave - adjudications (orchestrator)

Inputs: `planner-conformance.md` (1 high, 3 med, 1 low) and
`planner-adversarial.md` (findings 1-7), consolidated by the planner into 12
numbered findings. Every one verified against the live tree before
adjudication; the smithy transient-retry constants
(`TRANSIENT_ERROR_STATUS_CODES = [500, 502, 503, 504]`,
`@smithy/core/dist-cjs/submodules/retry/index.js:35`) were re-read to confirm
finding 5's premise. Fix wave = ONE fresh implementer; gates 1/2/3/5 re-run
after; gate 4 NOT re-run (touched files are `app/src/lib/dynamoAdmin.ts`,
`app/test/*`, and docs - inside the planner's stated exemption).

| # | sev | verdict | disposition |
|---|---|---|---|
| 1 | HIGH | ACCEPT - CONFIRMED by reading. The retried-tolerated DELETING path returns with no wait while the commonest caller (`importApply.integration.test.ts:72-77` + 7 more in-file pairs) re-creates the same name immediately; `ensureTable`'s mirror-image path polls, this one does not; case 4 pins the hole in. Fix symmetrically: a bounded `pollUntilTableGone` (DescribeTable until `ResourceNotFoundException`; other read failures count as "maybe still there"; same interval/ceiling shape and injection seams as `pollUntilTableActive`; on exhaustion rethrow the ORIGINAL `ResourceInUseException` with the observed status appended) - on the RETRIED-tolerated path only; the un-retried path stays byte-identical to main. Cases 23 (DELETING,DELETING,gone -> resolves, counts pinned) and 24 (never gone -> rethrows the RIU instance, message carries table + status). |
| 2 | MED | ACCEPT - planner-directed, and the argument is the module's own: the round-2 deadline reorder closed this exact blind spot one line earlier. New order in the catch: retryable? -> endpoint gate (lazy) -> verify ONCE per failed attempt INCLUDING the final -> attempt bound -> deadline -> onRetry -> sleep. The gate stays ahead of the hook so a non-local endpoint still gets today's behaviour exactly (zero extra sends; cases 11/12 unchanged). This deviates from the plan's ratified "hook NOT called on the final attempt" row - recorded here as planner-overridden; the consistency argument it rested on (`db-update-gsis.ts:117`) died with the old helper. Case 10 updated (4 sends, hook 4x, trailing re-read in the interleave, title updated); NEW case 25: `UpdateTimeToLive` fails 4x but the 4th re-read reports ENABLED -> resolves. Hook-contract comments updated everywhere they state the old rule. |
| 3 | MED | ACCEPT. Both polls (ACTIVE and the new GONE) get modest exponential backoff: interval doubles per read from the injected base, capped at 8x base (defaults: 100 -> 200 -> 400 -> 800 -> 800... ~= 16 reads in 10s instead of ~100). The 10s ceiling is UNCHANGED. Existing cases inject `intervalMs: 1` and assert script-driven counts, so they are unaffected. |
| 4 | HIGH | ACCEPT - CONFIRMED: `AGENTS.md` said "was not faster" against 189/165 vs 231/190, the negation of its own parenthesis, and `s5-clean-key.md:64-69` warned against exactly that sentence. Rewrite to what the data supports: the explicit-key arm was slightly FASTER on a quiet fresh container (189/165s vs 231/190s), tracking a declining e2e neighbour across the interleave, not the key scheme; the supersession rests on the code reading. |
| 5 | HIGH | ACCEPT-NARROW. The SDK premise is real (verified: 5xx classified TRANSIENT, 3 attempts default; `InternalServerError` carries no `$retryable` trait, so classification hangs on `$metadata.httpStatusCode`) and the container's actual status for these two faults is UNVERIFIABLE here - the faults cannot be summoned on demand (the anchor's own history: four probe shapes failed) and no recorded sighting captured `$metadata`. So: (a) narrow `dynamoAdmin.ts:88-92` to what the issue history proves - the faults ESCAPED to callers and failed gates; whether the SDK's transient retry had already fired underneath was never recorded; (b) correct the deadline arithmetic comment: the bound is per retried SEND, and one send may itself contain up to 3 nested SDK attempts if the container answers 5xx (unverified either way), so the lock signature may cost ~30s per helper attempt, not ~10s - size hook budgets pessimistically and do not derive them from this constant; (c) add one line to the anchor issue: the NEXT sighting must record `err.$metadata.httpStatusCode` and `err.$metadata.attempts`, which settle the nesting question for free. DECLINED: setting `maxAttempts` on the local client - that is `app/src/lib/dynamo.ts` (runtime code outside the mission's one permitted runtime file) and would change every LOCAL data-plane send's behaviour; filed in the handback as an open item instead. |
| 6 | MED | ACCEPT - CONFIRMED against our own S5 probe: `:164`'s "the two TTL legs do not run at all" is false for globalSetup (test.env reaches workers only - the entire basis of `globalsetup-reenables-ttl-on-shared-tables`). Rewrite: workers skip the TTL legs; globalSetup, `db:create` and the e2e lanes run them. |
| 7 | MED | ACCEPT - CONFIRMED: the 900s ceiling predates this branch (`1448b130`, 2026-08-16; our own `s1-retry.md:160-163` says untouched). Replace "which the same diff introduced" with "which predates this change (1448b130)". |
| 8 | MED | ACCEPT - CONFIRMED: 31.5s exists only in gitignored run state. Add a committed addendum to `measurements/s2-guard-cost.md`: logCallSiteGuard per-file durations inside the S0 full runs, read from the run logs - warm-up 12.6s (unlabelled), run 1 31.5s (host CPU 100%, 1 vitest neighbour + 2 e2e suites - the "worst loaded" the closure cites), run 2 10.0s, run 3 13.4s - with the S0 snapshot labels beside them. The closure and handback citations then point at committed text. |
| 9 | MED | ACCEPT - CONFIRMED (the C1 supersession is marked four paragraphs earlier while the bigger decoy reversal is not, and the code sends readers to a record that says "no decoys"). One bracketed C1-style sentence at the spec's "No traversal decoys" paragraph naming `b81ceb23` + r1-adjudications A3 + the 4/6-vs-0/6 reproduction; one dated bracketed note in `s3-static-smoke.md`'s traversal section saying the no-decoys text was true at `796b8632` and superseded by the wave-1 fix. Records stay as produced otherwise - these are marked addenda, not rewrites. |
| 10 | LOW | ACCEPT - CONFIRMED dead by construction (`identityHolds` is the conjunction; `ctx.skip` throws). Delete the two loops; the guard IS the test; one comment says so. |
| 11 | LOW | ACCEPT, with a DETERMINISTIC reshape instead of a wider ratio: script send 1 to fail fast (5ms) and send 2 to fail after 2500ms against `deadlineMs: 2000`, `attempts: 12`, zero backoff -> the deadline check after send 2 always reads elapsed >= 2505 > 2000, so EXACTLY 2 sends, asserted as equality plus the original-error identity. No lower-bound wall-clock race in either direction (the deadline cannot fire before send 2 - it is only read in the catch, and send 1 completes at ~5ms). Teeth: deadline removed -> 12 sends. |
| 12 | LOW | PARTIAL. ACCEPT for `dynamoAdmin.ts:270` (attach the swallowed verification error as `cause` on the rethrown original, only when it has no cause) and for `pollUntilTableActive`/`pollUntilTableGone` (carry the LAST read error, if any, as `cause` on the thrown poll error). DECLINE for `db-update-gsis.ts:80`: `indexStatus`'s swallow is the caller's documented fail-open tolerance and leads to a RE-SEND, not a throw - there is no surfaced error to attach a cause to; recorded here instead. |
| (conf 5) | LOW | ACCEPT. Replace the three U+2713 in `design-review/plan-r3-reviewer-c.md` with `OK` - the repo's added-lines ASCII rule; a reviewer's tick is not captured tool output. |

Gate 4 exemption invoked: no file outside `app/test/**`,
`app/src/lib/dynamoAdmin.ts`, and `.md` documentation is touched by this wave.
