# Spec-conformance review - CloudFront maintenance page

Date: 2026-09-07
Reviewer scope: Read-only Phase 4 review of the committed feature. No test suite was started.

## Verdicts

| Work-map item | Verdict | Evidence | Residual risk |
| --- | --- | --- | --- |
| S1 canonical page and renderer | CONFORMS | The sole five-key edge catalog holds the approved literal copy at `app/src/messages/edgeMaintenance.json:1`. The actual Terraform template has `lang`, viewport, CSP before content, the marker, a single main/h1/link, the required system styling, and no asset or script tags at `infra/modules/cloudfront/templates/maintenance.html.tftpl:1-47`. All copy interpolations use text/title escaping at lines 7 and 41-44. The renderer reads that catalog and template through Terraform `templatefile`, validates exact printable-ASCII keys, and verifies owned scratch cleanup at `e2e/support/maintenancePage.ts:8-72`. Focused tests exercise the real renderer, hostile escaping, malformed copy, and forbidden dependencies at `e2e/support/maintenancePage.test.ts:4-64`. | Rendering still depends on local Terraform being available; the helper intentionally fails rather than silently substituting a hand-written renderer. |
| S2 private origin, mapping, and preservation | CONFORMS | The dedicated account-scoped bucket, owner enforcement, four public-access blocks, SSE-S3, exact object metadata/hash, always-SigV4 OAC, and exact-object SourceArn policy are at `infra/modules/cloudfront/maintenance.tf:11-100`. The distribution adds an S3 REST origin without an app header at `infra/modules/cloudfront/main.tf:124-128`, one exact read-only CachingDisabled behavior at lines 189-197, and only 502/504 status-preserving zero-TTL responses at lines 213-225. `maintenance_path` references the object while the policy references the distribution ARN, preserving the intended acyclic direction at `maintenance.tf:15,95-98`. Existing app/default/media contracts are asserted at `infra/modules/cloudfront/tests/maintenance.tftest.hcl:113-190`; five mocked Terraform faults cover extra 503, false 200, wide S3 read, media OAC removal, and maintenance OAC removal, while the default-forwarding fault is a separately labeled in-process source assertion at `scripts/check-maintenance-infra.mjs:115-158`. Deterministic inspection found exactly two custom error mappings and no 503 mapping. | The first apply has the documented policy-propagation interval; hosted S3 access and CloudFront substitution require the separately authorized operator validation. |
| S3 browser recovery and API behavior | CONFORMS | The API characterization test keeps 502/504 HTML as status-bearing `ApiError`s, preserves the three typed JSON 503 codes, and checks successful JSON at `dashboard/src/api/client.maintenance.test.ts:6-43`, matching the unchanged request behavior at `dashboard/src/api/client.ts:118-130`. The hermetic browser spec uses the actual S1 renderer, covers 502/504 x GET/POST x 320/1280, checks copy, marker, title, overflow, 200 percent text enlargement, no dependencies, focus, and GET-root recovery without query/body replay at `e2e/tests/dashboard-next/maintenance-page.spec.ts:12-101`. The existing relay ambiguity and push 503 readers remain unchanged at `dashboard/src/routes/contact/CreateRelayGroupModal.tsx:152-163` and `dashboard/src/routes/settings/useNotifications.ts:98-106`. | Route fulfillment proves browser behavior with the real document, not deployed CloudFront substitution. |
| S4 runbook and verification boundary | CONFORMS | `RUNBOOK.md:43-59` describes selected-status scope, distribution-wide consequences, fresh GET recovery, dev-before-prod guarded provisioning, direct-page limitations, separately authorized substitution proof, and mapping-only rollback. `e2e/README.md:61-73` documents Terraform and hermetic focused checks, five broken Terraform module copies plus separately labeled in-process source fixtures, composition validation, and the no-live-plan/apply boundary. Neither deploy code nor the outbound catalog references the feature, confirmed by deterministic source inspection. | The runbook correctly leaves dev/prod plan/apply, propagation, and real 502/504 substitution evidence to the human operator after merge. |

## Findings

No must-fix, partial, or missing work-map item was found. The maintenance source guard is substantive: the checker restores its baseline after each mutation and rejects a changed default forwarding policy through an anchored source assertion when the provider mock cannot preserve that distinction (`scripts/check-maintenance-infra.mjs:55-72,139-158`).
